<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MST</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
    <link rel="icon" type="image/png" href="/images/icon.png">
    <style>
        body {
            width: 100%;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;

        }

        .content {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
            margin-top: 50px;
            /* overflow-x:scroll ; */
            width: 100%;
            height: 100%;
            gap: 10px;
        }

        .card {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            width: auto;
            height: auto;
            padding: 50px;
            /* flex-wrap: wrap; */
            /* background: rgba(255, 255, 255, 0.4); */
            /* background: #cad4d8; */
            background: #f2f7fb;
            border-radius: 20px;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1),
                5px 10px 15px rgba(0, 0, 0, 0.1),
                10px 15px 205px rgba(0, 0, 0, 0.1),
                15px 20px 25px rgba(0, 0, 0, 0.1),
                20px 25px 30px rgba(0, 0, 0, 0.1),
                25px 30px 25px rgba(0, 0, 0, 0.1),
                inset 2px 2px rgba(255, 255, 255, 0.2),
                inset -2px -2px rgba(0, 0, 0, 0.2);
            cursor: pointer;
            transition: 0.5s;
            text-decoration: none;
        }

        .card:hover,
        .back:hover {
            transform: scale(1.2);
        }

        .back {
            padding: 5px 10px;
            background: #f2f7fb;
            border-radius: 5px;
            text-decoration: none;
            color: rgb(43, 60, 85);
            transition: 0.5s;
        }
        .mst
        {
            color: #fff;
            font-size: 50px;
            font-weight: 900;
            text-transform: capitalize;
            margin-top:50px ;
        }
        .mst.no{
            color: #ffffffc4;
            font-size: 30px;
            font-weight: 900;
            text-transform: capitalize;
            margin-top:50px ;
            font-style: oblique;
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
        <h2 class="mst"><%=msts%></h2>
        <div class="content">
            <% if(len!=0) {arr.forEach((item,i=1)=>{%>
                <a class="card" href="/docs/<%=sub%>/<%=msts%>/<%=item%>">PDF<%=i+1%></a><br><br>
                <%})} else{%>
                    <h2 class="mst no">No Paper Yet</h2>
                    <%}%>
        </div>
        <br><br>
        <a class="back" href="/dept/<%= back%>">back to home</a>
        <br><br>
        <h1></h1>
</body>

</html>